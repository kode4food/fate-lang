# File Resolver's Import should attempt to load a file.int before venturing
# into directories.  Therefore this module should be the one exposed by a
# File Resolver

let test_value = "right!"

export test_value
